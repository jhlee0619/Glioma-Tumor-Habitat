# Core deep learning (required to load and run the deposited dVAE)
torch>=2.0
pytorch-lightning>=2.0
omegaconf>=2.3
einops>=0.7

# Medical imaging I/O
antspyx>=0.4
nibabel>=5.0

# Random Forest classifiers (deposited as joblib)
scikit-learn>=1.7
joblib>=1.3

# Numerics
numpy>=1.24
scipy>=1.10

# Statistical tests reported in Table 2 / Table 3 (Kruskal-Wallis and
# Mann-Whitney U are provided by SciPy; statsmodels supplies the DeLong
# test and the Benjamini-Hochberg P-value adjustment)
statsmodels>=0.14

# Plotting (imported by taming.models.vqgan_sep at module load; kept even
# though inference does not draw figures)
matplotlib>=3.7

# Training-run logging (imported at module load by taming.models.vqgan_sep;
# not exercised during inference)
wandb>=0.16

# Utilities
tqdm>=4.65
